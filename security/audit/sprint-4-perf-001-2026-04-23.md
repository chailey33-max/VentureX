# Sprint 4 PERF-001 Verification

Date: 2026-04-23
Scope: Bundle and startup cost remediation

## 1) Route/feature code splitting for heavy sections

Status: PASS

Evidence:
- AI generation service moved to dynamic import usage in `src/App.tsx`:
  - `import('./services/geminiService')` in on-demand handlers.
- Build now emits separate async chunk:
  - `dist/assets/geminiService-*.js`

## 2) Lazy-load admin and premium-only surfaces

Status: PASS

Evidence:
- Premium AI actions now lazy-load `geminiService` only on interaction.
- Admin-only cloud sync helpers now lazy-load Firestore APIs (`writeBatch`, `getDocs`) in `src/App.tsx`.

## 3) Configure chunking strategy for large modules

Status: PASS

Evidence:
- Added manual chunk strategy in `vite.config.ts` for:
  - React
  - Motion
  - Firebase
  - Icons
  - Misc vendor
- Build output confirms split vendor chunks instead of single monolith.

## 4) Add bundle size budget checks in CI

Status: PASS

Evidence:
- Added bundle budget script:
  - `scripts/check-bundle-budget.mjs`
- Added package scripts:
  - `check:bundle-budget`
  - `build:with-budget`
- Added CI workflow:
  - `.github/workflows/bundle-budget.yml`

## 5) Defer non-critical startup work to idle or delayed phases

Status: PASS

Evidence:
- Deferred non-critical modified-id hydration using idle-time scheduling in `src/App.tsx` (idle callback with timeout fallback).

## Metrics snapshot

- Previous single main bundle: ~1,200 kB minified (pre-split baseline).
- Current split output:
  - `index-*.js`: ~393 kB
  - `vendor-firebase-*.js`: ~616 kB
  - `vendor-react-*.js`: ~200 kB
  - `vendor-motion-*.js`: ~130 kB
  - `geminiService-*.js`: ~1.4 kB async chunk
- Budget check:
  - largest JS chunk `615856` bytes vs threshold `950000` bytes => PASS
